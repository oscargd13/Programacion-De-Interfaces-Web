# Practica 4 - Formulario


    Para ejecutar el frontend utilizaremos el comando "npm start" en la terminal
    Para ejecutrar el backend utilizaremos el comando "npx ts-node server.ts" desde la carpeta src.

  